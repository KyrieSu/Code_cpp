#include <iostream>
#include <cstring>
#include <queue>
using namespace std;

int main(){
    int N; //number of dots
    cin >> N;
    vector<vector<int> > vec(N);
    cout << vec.size();
    return 0;
}